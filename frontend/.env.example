# LifeLoan Frontend Environment Configuration (Template)
# The React/Vite frontend communicates with the FastAPI backend.
# Gemini API calls are securely executed server-side only in backend/.env.
# DO NOT put your real GEMINI_API_KEY in frontend client-side environment files!

VITE_API_URL="http://localhost:8000"
